
import { pluralizeString } from 'inventory/helpers/pluralize-string';
import { module, test } from 'qunit';

module('Unit | Helper | pluralize string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pluralizeString([42]);
  assert.ok(result);
});

