
import { singularizeString } from 'inventory/helpers/singularize-string';
import { module, test } from 'qunit';

module('Unit | Helper | singularize string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = singularizeString([42]);
  assert.ok(result);
});

