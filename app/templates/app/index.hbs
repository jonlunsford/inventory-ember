<div class="container">
  <div class="row">
    <div class="col s12 m8 offset-m2">

      {{#md-card
        buttonText='Logout'
        onsubmit=(route-action 'logout')
        title='Welcome to Inventory IO!'}}

        {{#user-info as |u|}}
          Thanks for logging in <strong>{{u.email}}</strong>
        {{/user-info}}
      {{/md-card}}
    </div>
  </div>
</div>
